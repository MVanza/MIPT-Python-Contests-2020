И ещё раз доработайте первую задачу. Реализуйте следующее:

Смысл класса Car остаётся без изменений.
Теперь машины умеют сравниваться между собой. Одинаковыми являются машины, у
которых совпадает номер. Остальные параметры при сравнении не важны.
